**프로젝트 명칭:** (가칭) 사내 데이터 연동 AI 어시스턴트 MCP 서버 구축

**1. 프로젝트 목표:**
사내 개발팀 구성원들이 코드 작성 및 디자인 작업 시 필요한 정보를 효율적으로 얻고, 반복적인 작업을 자동화할 수 있도록 지원하는 MCP(Model Context Protocol) 서버를 구축한다. 이 서버는 GitLab, Notion, Figma 등 사내에서 사용 중인 주요 도구들과 연동하여 컨텍스트 기반의 정보 제공 및 자동 생성 기능을 수행한다.

**2. 대상 사용자:**
사내 개발팀 구성원 (개발자, 기획자, 디자이너 등)

**3. 핵심 기능 요구사항:**

* **코드 컨텍스트 기반 추천 (GitLab 연동):**
    * **사용자 요청:** 개발자가 코드 에디터 클라이언트(예: Claude Client, 기타 MCP 클라이언트)를 통해 변수명, 쿼리문 등에 대한 추천을 요청한다.
    * **서버 동작:**
        * MCP 서버는 사용자의 요청 컨텍스트(현재 작업 중인 코드, 커서 위치 등)를 파악한다.
        * 연결된 GitLab API (또는 GitLab 전용 MCP 서버)를 통해 관련 프로젝트의 소스 코드를 분석한다.
        * 분석 결과를 바탕으로 적절한 변수명 또는 쿼리 구조를 추천하여 클라이언트에 응답한다.
    * **데이터 소스:** GitLab 저장소 내 소스 코드
    * **주요 기술:** GitLab API 연동, 코드 분석 로직(LLM 활용 가능), MCP 통신

* **문서 기반 디자인 파일 자동 생성 (Notion & Figma 연동):**
    * **사용자 요청:** 사용자가 클라이언트를 통해 특정 Notion 페이지(예: "VC 정책" 페이지)를 기반으로 Figma 디자인 파일 생성을 요청한다.
    * **서버 동작:**
        * MCP 서버는 요청된 Notion 페이지의 내용을 Notion API를 통해 가져온다.
        * 가져온 텍스트, 구조, (가능하다면) 이미지 등의 콘텐츠를 분석한다.
        * 분석된 내용을 바탕으로 Figma API를 호출하여 기본적인 디자인 요소(텍스트 블록, 이미지 배치 등)로 구성된 Figma 파일을 생성한다.
        * 생성된 Figma 파일의 링크 또는 정보를 클라이언트에 응답한다.
    * **데이터 소스:** Notion 페이지 콘텐츠
    * **주요 기술:** Notion API 연동, Figma API 연동, 콘텐츠 구조 분석 및 매핑 로직, MCP 통신

**4. 시스템 아키텍처 (제안):**

* **클라이언트:** 사용자가 상호작용하는 인터페이스 (IDE 플러그인, 챗봇 인터페이스 등). MCP 클라이언트 역할을 수행하며 서버에 요청을 보낸다.
* **중앙 MCP 서버:**
    * 클라이언트로부터 요청을 수신하고, 요청 유형에 따라 적절한 로직을 수행한다.
    * 외부 서비스(GitLab, Notion, Figma) API와의 연동 및 데이터 처리를 담당한다.
    * Go 언어로 개발하여 성능과 동시성 처리 능력을 확보한다.
* **외부 서비스 API:** GitLab API, Notion API, Figma API

**5. 기술 스택 (권장):**

* **MCP 서버 개발 언어:** Go
* **핵심 라이브러리/프레임워크:**
    * Go 표준 라이브러리 (net/http 등)
    * 각 서비스별 Go API 클라이언트 라이브러리 (GitLab, Notion, Figma)
    * (선택적) LLM 연동 라이브러리 (코드 분석/생성 보조)
    * (선택적) LangChain 또는 유사 프레임워크 (RAG 등 활용 시)

**6. 주요 고려 사항:**

* **인증 및 권한 관리:** 각 외부 서비스(GitLab, Notion, Figma) API 접근을 위한 안전한 인증 방식 (OAuth, API 토큰 등) 및 사용자별 접근 권한 관리가 필요하다.
* **API Rate Limit 처리:** 각 서비스의 API 호출 제한을 고려한 설계가 필요하다.
* **데이터 매핑:** Notion 콘텐츠 구조를 Figma 디자인 요소로 변환하는 규칙 및 유연성 확보 방안이 필요하다.
* **오류 처리:** API 호출 실패, 데이터 파싱 오류 등 다양한 예외 상황에 대한 견고한 오류 처리 로직이 필요하다.
* **확장성:** 향후 Jira 등 다른 도구 연동을 고려한 모듈식 설계가 권장된다.
